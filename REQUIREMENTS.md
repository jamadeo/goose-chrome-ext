A chrome extension to open a Github PR in Goose.

The extension should render a button hovering in the upper right with the goose logo. Clicking it should open `goose://new-session`.

The button should render only when visiting a Github PR page (github.com/<org>/<repo>/pull/<pr>).
